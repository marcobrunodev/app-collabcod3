import { createGlobalStyle } from 'styled-components'

const Sizes = createGlobalStyle`
  :root {
    --tile: 1.6rem;
    --tile-npc: 4.8rem;
    --tile-icon-button: 3.2rem;
  }
`

export default Sizes
